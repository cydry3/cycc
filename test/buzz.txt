int abs(int x) {
  if (x < 0)
    return -x;
  return x;
}

int main() {
  int x;
  int y;

  x = 0;
  y = -1;
  // 行コメント
  // y = 1;
  x = y + 5;
  /*
    コメントブロック
    y = 1;
    x = y + 3;
  */

  return abs(x);
}
